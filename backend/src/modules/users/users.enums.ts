export enum LoginType {
  SUPER_ADMIN = 'superAdmin',
  ADMIN = 'admin',
  STAFF = 'staff',
  VISITOR = 'visitor',
}

export enum UserStatus {
  ACTIVE = 'active',
  RESTRICTED = 'restricted',
  BLOCKED = 'blocked',
  DISABLED = 'disabled',
}
